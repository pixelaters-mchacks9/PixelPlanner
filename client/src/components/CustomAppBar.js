import React from 'react';
import styled from 'styled-components';
import {
  AppBar,
  Toolbar,
  TextField,
  Button,
  List,
  ListItem,
  Divider
} from 'react95';

export default class CustomAppBar extends React.PureComponent {
  constructor(props) {
    super(props);
     this.state = {
      loading: true
    };
  }

}


